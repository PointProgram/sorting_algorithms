#ifndef __GLOBALVARIABLES_H__
#define __GLOBALVARIABLES_H__

// extern global variable that is used throughout the whole project for defining blocks size of the 3D array
extern int BLOCK;
// extern global variable that is used throughout the whole project for defining rows size of the 3D array
extern int ROW;
// extern global variable that is used throughout the whole project for defining columns size of  the 3D array
extern int COLUMN;
// extern global variable that is used throughout the whole project for defining vector lengt of the 1D array
extern int VECTOR_LENGTH;

#endif

